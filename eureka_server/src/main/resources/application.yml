spring:
  application:
    name: eureka-server
  cloud:
    service-registry:
      auto-registration:
        enabled: false
server:
  port: 8083
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false